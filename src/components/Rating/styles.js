// @flow
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  MainContainer: {
    flexDirection: 'row',
  },
  childView: {
    justifyContent: 'center',
    flexDirection: 'row',
  },
});
